allOf:
  - $ref: '#/components/schemas/account_detail'
  - type: object
    properties:
      latest_starchats_version:
        type: string
        description: Latest version of Starchats available
        example: "3.0.0"
      subscribed_features:
        type: array
        items:
          type: string
        description: List of subscribed starchat features (if starchat edition is enabled)